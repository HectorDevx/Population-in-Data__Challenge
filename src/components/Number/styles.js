import styled from "styled-components";

export const NumberContainer = styled.div`
  margin: var(--space-mini) auto;
  display: flex;
  align-items: center;
  color: var(--color-blue);
  img {
    max-width: var(--space-medium);
  }
`;
