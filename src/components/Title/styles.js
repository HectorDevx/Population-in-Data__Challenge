import styled from "styled-components";

export const TitleContainer = styled.div`
  .center {
    text-align: center;
  }

  .left {
    text-align: left;
  }

  p,
  h3 {
    color: var(--color-blue);
  }
`;
